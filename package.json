{
  "name": "whatsapp-assistant",
  "private": true,
  "version": "0.3.0",
  "description": "Local, read-only WhatsApp Web bridge powered by Baileys",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "check": "node --check src/server.js && node --check src/mirror-store.js && node --check src/chat-coverage.js && node --check src/runtime-paths.js && node --check src/launch-agent.js && node --check src/systemd-service.js && node --check bin/wa.js",
    "test": "node --test"
  },
  "bin": {
    "wa": "./bin/wa.js"
  },
  "files": [
    "bin",
    "src",
    "docs",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "baileys": "7.0.0-rc13",
    "pino": "^9.6.0",
    "qrcode": "^1.5.4",
    "qrcode-terminal": "^0.12.0"
  }
}
